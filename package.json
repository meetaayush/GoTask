{
  "name": "gotask",
  "type": "module",
  "source": [
    "./assets/base.css",
    "./assets/index.ts"
  ],
  "targets": {
    "default": {
      "distDir": "./static"
    }
  },
  "scripts": {
    "dev": "rm -rf .parcel-cache && rm -rf static && parcel build"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.12.0",
    "@types/bun": "latest",
    "parcel": "^2.12.0",
    "postcss": "^8.4.41",
    "tailwindcss": "^3.4.10"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
